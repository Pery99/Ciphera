[build]
builder = "RAILPACK"
buildCommand = "npm run build"
watchPatterns = ["apps/api/**", "packages/**"]

[deploy]
releaseCommand = "npm run deploy:db --workspace @ciphera/api"
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10